{{outlet}}

<div class="container">
	<div class="row">
	{{partial "partials/side"}}
		<div class="col-sm-6">
			
			
				<form {{action "formUpdate" on="submit"}} >
						<div class="form-group">
							<label for="">Name</label>
							{{input type="text" value=model.name class="form-control"}}	
						</div>
						<div class="form-group">
							<label for="">Date</label>
							{{input type="date" value=model.date class="form-control"}}
						</div>
						<div class="form-group">
							<label for="">Reasons</label>
							{{textarea value=model.reasons class="form-control"}}
						</div>
						<button class="btn btn-sm btn-primary">Update</button>
							
						
				</form>
		</div>
	</div>
</div>